#pragma once

#include "Math\Vectors\Vector2.hpp"
#include "Math\Vectors\Vector3.hpp"

using namespace math;

/// @brief shorthand for writing vec2<float>
using vec2f = math::vec2<float>;
/// @brief shorthand for writing vec2<double>
using vec2d = math::vec2<double>;

/// @brief shorthand for writing vec3<float>
using vec3f = math::vec3<float>;
/// @brief shorthand for writing vec3<double>
using vec3d = math::vec3<double>;
